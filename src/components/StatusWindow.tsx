// Deprecated. The floating overlay was replaced by an inline, injected game
// bar (see components/GameBar.tsx + lib/patchLibraryApp.tsx), which keeps
// gamepad focus inside the page and never overlaps the Steam Deck button bar.
export {};
