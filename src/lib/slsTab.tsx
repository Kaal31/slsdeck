// Removed. The experimental "SLS Games" library tab was pulled at the user's
// request. Its toggle, boot/unload wiring, callables and settings keys are all
// gone. Collections ("Group SLS games into a collection") are a separate feature
// and remain intact. This file is intentionally empty (kept only because the
// mount blocks deletion) and is not imported anywhere; excluded from the zips.
export {};
