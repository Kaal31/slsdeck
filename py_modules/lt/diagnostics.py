# Intentionally removed. The scrubbed-log diagnostics collector only made sense
# for a public tool taking bug reports from users you can't see. This build is
# private and single-user, so it's dead weight. Kept as an empty stub only
# because the mount blocks file deletion; excluded from the packaged zips.
