# Intentionally empty. Charon-Database is already integrated in downloads.py
# (fetch_lua_text + _try_charon_manifest, using the same BlissBlender/Charon-Database
# database-1/2 {appid}.lua URLs). This standalone module was redundant and is not
# imported anywhere. Kept as an empty stub only because the mount blocks deletion.
